<%= form_with(model: emoji) do |form| %>
  <% if emoji.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(emoji.errors.count, "error") %> prohibited this emoji from being saved:</h2>

      <ul>
        <% emoji.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :reaction, style: "display: block" %>
    <%= form.text_field :reaction %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
